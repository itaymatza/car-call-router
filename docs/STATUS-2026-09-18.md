# Car Call Router — checks performed on 2026-09-18

## Results actually obtained

| Check | Result |
|---|---|
| Recovered corrected production Kotlin source matches prior candidate | Byte-for-byte match |
| Pure Kotlin policy cases | 62/62 passed |
| Production service exercised with Android framework doubles | 37/37 passed |
| Seeded state-machine exploration | 5,000 traces, 250,000 transitions passed |
| Shell-script syntax | Passed |
| GitHub contents upload | HTTP 403: Resource not accessible by integration |
| GitHub Git Trees upload | HTTP 403: Resource not accessible by integration |
| Repository after attempts | Only its original 17-byte README.md |
| New Android APK build | Not performed: no local SDK; dependency hosts unavailable; CI upload blocked |
| Android emulator / installation | Not performed |
| Samsung Telecom admission / BMW Bluetooth audio / microphone / Android Auto coexistence | Not verified |

## Local changes in this source package

- Marked the corrected source as 0.2.1-device-test, version code 4, to avoid confusing it with the old 0.2.0 APK.
- Fixed the standalone service-test launcher's default source path and put temporary compiled output outside the source tree.
- Added tools/test-all.sh and reran all three suites through it.
- Included fresh raw logs, toolchain information and both rejected upload attempts.
- Excluded the historical APK, which predates the callback-latching correction.
- Replaced the stale packaging README with an explicit status and architectural limitations.

The production app implementation was recovered unchanged, not rewritten. It uses a Kotlin/framework-XML UI and Telecom.requestBluetoothAudio for address-specific routing; it is not a completed Compose/endpoint-request-only implementation.

The existing Gradle project and GitHub Actions workflow are retained but have not been executed for this new candidate. All test results above are host-JVM checks with explicit limitations. The included historic reports describe earlier work and are not new test results.

## Publication blocker

The actual connector write calls returned HTTP 403. Account-level push/admin metadata did not prove that the integration could write. No files, commits, pull requests, release assets or workflows were published in this session.

OpenAI's GitHub connection documentation separately describes the ChatGPT GitHub app as read-only and directs code publishing to Codex:
https://help.openai.com/en/articles/11145903-connecting-github-to-chatgpt

Do not treat changing ChatGPT's confirmation preference as granting missing GitHub write authority. No credentials or GitHub permissions were changed.
